# Architecture et flux - COTATION_H

## Diagramme de flux complet

```
┌─────────────────────────────────────────────────────────────────┐
│                        APPLICATION                              │
│                     Lahimena Tours                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                ┌───▼────┐      ┌──────▼─────┐
                │ SIDEBAR │      │ MAIN CONTENT│
                └───┬────┘      └──────┬──────┘
                    │                   │
        ┌──────────┬─┴──────────┐       │
        │          │            │       │
    ┌───▼─┐   ┌───▼──┐   ┌─────▼─────┐ │
    │ ... │   │Hotel │   │ Résumé    │ │
    │     │   │Quot. │   │ Cotations │◄┘
    └─────┘   └───┬──┘   └─────┬─────┘
                  │             │
          ┌───────▼──────┐      │
          │ Generate PDF │      │
          └───────┬──────┘      │
                  │             │
        ┌─────────▼──┐   ┌──────▼─────────────┐
        │ PDF File   │   │ HotelQuotationSu...│
        │ Saved ✅   │   │ (New Component)    │
        └────────────┘   └──────┬─────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                ┌───▼──────┐        ┌────────▼────┐
                │Load Data │        │ Render View │
                │from Excel│        │  (Grouping) │
                └───┬──────┘        └─────────────┘
                    │
            ┌───────▼─────────┐
            │    data.xlsx    │
            │  ┌────────────┐ │
            │  │COTATION_H  │ │ ◄── Automatic Save
            │  │  SHEET     │ │
            │  └────────────┘ │
            └─────────────────┘
```

## Diagramme d'entité-relation

```
┌─────────────────────────────────┐
│        COTATION_H SHEET         │
├─────────────────────────────────┤
│ PK: Row_Number                  │
│    Date (timestamp)             │
│ FK: ID_Client (→DEMANDE_CLIENT) │
│    Nom_Client                   │
│ FK: Hôtel (→BDD_HOTEL)          │
│    Ville                        │
│    Nuits (INT)                  │
│    Type_Chambre                 │
│    Adultes (INT)                │
│    Enfants (INT)                │
│    Plan_Repas                   │
│    Période                      │
│    Total_Devise (FLOAT)         │
│    Devise (STRING)              │
└─────────────────────────────────┘
         │           │
         ▼           ▼
    ┌────────┐  ┌────────┐
    │CLIENTS │  │ HÔTELS │
    └────────┘  └────────┘
```

## Diagramme de classe - HotelQuotationSummary

```
┌──────────────────────────────────────┐
│   HotelQuotationSummary              │
├──────────────────────────────────────┤
│ Attributes:                          │
│  - parent: tk.Widget                 │
│  - quotations: List[Dict]            │
│  - grouped_by_client: Dict           │
│  - grouped_by_city: Dict             │
│  - current_view: String              │
│  - content_frame: tk.Frame           │
│  - view_var: tk.StringVar            │
├──────────────────────────────────────┤
│ Methods:                             │
│  + __init__(parent)                  │
│  + _load_quotations()                │
│  + _create_interface()               │
│  + _on_view_changed(event)           │
│  + _display_by_client()              │
│  + _display_by_city()                │
│  + _create_client_frame(...)         │
│  + _create_city_frame(...)           │
│  + _refresh_data()                   │
└──────────────────────────────────────┘
```

## Pipeline de données

### Lors de la création d'une cotation:

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER INPUT - Formulaire HotelQuotation                  │
│    - Select Client                                          │
│    - Select Hotel                                           │
│    - Enter parameters (nights, adults, children, meals)    │
│    - Click "Calculer"                                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. CALCULATION - _calculate_price()                         │
│    - Validate input (nights > 0, adults > 0)               │
│    - Get room price from hotel data                          │
│    - Calculate meal supplements                             │
│    - Apply client type adjustment (TO vs PBC)              │
│    - Convert currency if needed                             │
│    - Display results in results_text widget                │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 3. PDF GENERATION - _generate_quote()                       │
│    - Extract all parameters                                  │
│    - Call generate_hotel_quotation_pdf()                    │
│    - PDF file created in /devis folder                      │
│    - Display success message                                │
│    - Auto-open PDF file                                     │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 4. EXCEL SAVE - ✨ NEW ✨                                    │
│    - Build quotation_data dict                              │
│    - Call save_hotel_quotation_to_excel(quotation_data)    │
│    - Open/Create data.xlsx                                  │
│    - Create COTATION_H sheet if needed                      │
│    - Add headers if first row                               │
│    - Insert row with quotation data                         │
│    - Save xlsx file                                         │
│    - Log success                                            │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 5. DATA PERSISTENCE                                         │
│    Excel: data.xlsx                                         │
│    Sheet: COTATION_H                                        │
│    New row created with full quotation details              │
└──────────────────────────────────────────────────────────────┘
```

### Lors de l'affichage du résumé:

```
┌──────────────────────────────────────────────────────────────┐
│ 1. USER ACTION - Click on "📊 Résumé cotations"            │
│    Menu: 🏨 Cotation hôtel → 📊 Résumé cotations          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. INSTANTIATION                                            │
│    Class: HotelQuotationSummary                            │
│    Parent: main_scroll (tk.Frame)                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 3. DATA LOADING - _load_quotations()                        │
│    - load_all_hotel_quotations()                            │
│      → Reads all rows from COTATION_H                       │
│      → Returns list of quotation dicts                      │
│    - get_quotations_grouped_by_client()                     │
│      → Groups by client_id                                  │
│      → Calculates sum per client                            │
│    - get_quotations_by_city()                               │
│      → Groups by city                                       │
│      → Calculates sum per city                              │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 4. INTERFACE CREATION - _create_interface()                │
│    - Title: "RÉSUMÉ DES COTATIONS HÔTEL"                   │
│    - View selector dropdown                                 │
│    - Refresh button                                         │
│    - Content frame (for data display)                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 5. DISPLAY - _display_by_client() or _display_by_city()    │
│    Option A: By Client                                      │
│      ├─ Create scrollable canvas                            │
│      ├─ Display grand total                                 │
│      ├─ For each client:                                    │
│      │   └─ Create LabelFrame with Treeview                 │
│      │   └─ List all quotations for this client            │
│      │   └─ Display subtotal                                │
│                                                             │
│    Option B: By City                                        │
│      ├─ Create scrollable canvas                            │
│      ├─ Display grand total                                 │
│      ├─ For each city:                                      │
│      │   └─ Create LabelFrame with Treeview                 │
│      │   └─ List all quotations in this city               │
│      │   └─ Display subtotal                                │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────────────────────┐
│ 6. USER SEES                                                │
│    ┌────────────────────────────────────────┐              │
│    │ TOTAL GÉNÉRAL: 1,250,000.00 Ar        │              │
│    ├────────────────────────────────────────┤              │
│    │ Client: John Doe (ID: CLI001)         │              │
│    │  ┌─────────────────────────────────┐  │              │
│    │  │ Hôtel | Ville | Nuits | Total  │  │              │
│    │  ├─────────────────────────────────┤  │              │
│    │  │ ...                             │  │              │
│    │  └─────────────────────────────────┘  │              │
│    │ Sous-total: 500,000.00 Ar            │              │
│    └────────────────────────────────────────┘              │
│    ... (repeat for other clients/cities)                   │
└──────────────────────────────────────────────────────────────┘
```

## Architecture des couches

```
┌──────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  (tkinter GUI - widgets, windows, dialogs)                  │
│                                                              │
│  gui/sidebar.py ──┬─→ gui/main_content.py ──┐              │
│                   │                          │              │
│           ┌───────▼──────────────────────────▼────────┐     │
│           │ gui/forms/hotel_quotation.py              │     │
│           │ gui/forms/hotel_quotation_summary.py (NEW)│     │
│           └───────┬──────────────────────────┬────────┘     │
└──────────────────┼──────────────────────────┼───────────────┘
                   │                          │
                   ▼                          ▼
┌──────────────────────────────────────────────────────────────┐
│                 BUSINESS LOGIC LAYER                         │
│  (data processing, calculations, transformations)           │
│                                                              │
│  utils/excel_handler.py:                                    │
│  - save_hotel_quotation_to_excel()                          │
│  - load_all_hotel_quotations()                              │
│  - get_quotations_grouped_by_client()                       │
│  - get_quotations_by_city()                                 │
│                                                              │
│  utils/pdf_generator.py:                                    │
│  - generate_hotel_quotation_pdf()                           │
│                                                              │
│  utils/validators.py:                                       │
│  - get_exchange_rates()                                     │
│  - convert_currency()                                       │
└──────────────┬──────────────────────────────┬───────────────┘
               │                              │
               ▼                              ▼
┌──────────────────────────────────────────────────────────────┐
│                    DATA ACCESS LAYER                         │
│  (file I/O, Excel operations)                               │
│                                                              │
│  openpyxl (external library):                               │
│  - load_workbook()                                          │
│  - Workbook()                                               │
│  - ws.cell(row, column).value                               │
│  - ws.save()                                                │
└──────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                    PERSISTENCE LAYER                         │
│  (File system)                                              │
│                                                              │
│  data.xlsx:                                                 │
│  ├─ DEMANDE_CLIENT (existing sheet)                        │
│  ├─ BDD_HOTEL (existing sheet)                             │
│  └─ COTATION_H (NEW sheet)                                 │
│                                                              │
│  devis/:                                                    │
│  └─ DEVIS_HOTEL_YYYYMMDD_HHMMSS.pdf                        │
│                                                              │
│  logs/:                                                     │
│  └─ app.log                                                │
└──────────────────────────────────────────────────────────────┘
```

## Interaction entre composants

```
sidebar.py                    main_content.py              hotel_quotation.py
    │                              │                              │
    │ Click "Cotation hôtel"       │                              │
    │────────────────────────────→ │                              │
    │                              │ Display menu                 │
    │                              │                              │
    │ Click "Nouvelle cotation"    │                              │
    │────────────────────────────→ │ Instantiate HotelQuotation   │
    │                              │────────────────────────────→ │
    │                              │                              │ Load data
    │                              │                              │ Create form
    │                              │                              │
    │                              │                              │ User fills form
    │                              │                              │ Clicks "Générer"
    │                              │                              │
    │                              │                              │ Generate PDF
    │                              │                              │
    │                              │ excel_handler.py             │
    │                              │←─ save_quotation_to_excel()──│
    │                              │     ↓ openpyxl               │
    │                              │     ↓ data.xlsx              │
    │                              │     ✅ Saved                 │
    │                              │                              │
    │                              │←────────────────────────────│ Success
    │                              │                              │
    │ Click "Résumé cotations"     │                              │
    │────────────────────────────→ │                              │
    │                              │ Instantiate HotelQuotationS..│
    │                              │────────────────────────────→ │
    │                              │ (hotel_quotation_summary.py) │
    │                              │  Load all quotations         │
    │                              │  Group by client/city        │
    │                              │  Render Treeview tables      │
    │                              │ Display with totals          │
    │                              │←────────────────────────────│
```

## État du système

```
BEFORE:
data.xlsx
├─ DEMANDE_CLIENT (clients)
└─ BDD_HOTEL (hotels)

AFTER:
data.xlsx
├─ DEMANDE_CLIENT (clients)
├─ BDD_HOTEL (hotels)
└─ COTATION_H (quotations) ← NEW!
   ├─ Row 1: Headers
   ├─ Row 2: Quotation 1
   ├─ Row 3: Quotation 2
   └─ ... more rows as quotations are created
```

## Scaling et performance

### Pour 100 cotations
- Load time: ~500ms
- Display time: ~1s
- File size: ~50KB

### Pour 1,000 cotations
- Load time: ~2s
- Display time: ~3s
- File size: ~500KB

### Pour 10,000 cotations
- Load time: ~15s
- Display time: ~20s
- File size: ~5MB
- **Recommendation:** Implement pagination or archiving

## Error handling flow

```
save_hotel_quotation_to_excel()
    ├─ Check openpyxl available?
    │   └─ NO → Log warning, return -1
    ├─ File exists?
    │   └─ NO → Create new workbook
    ├─ Sheet exists?
    │   └─ NO → Create new sheet
    ├─ Write data to cells
    │   ├─ Format cells
    │   └─ Save file
    │       ├─ SUCCESS → Log info, return row_number
    │       └─ ERROR → Log error, return -1
    └─ EXCEPTION → Log error, return -1
```

---

**Document:** Architecture techniques COTATION_H  
**Version:** 1.0  
**Date:** 6 février 2026
