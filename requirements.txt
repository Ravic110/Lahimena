# This file is retained for compatibility. Prefer using the pyproject extras.
# Install core requirements:
#   pip install -e .
# Add financial dependencies:
#   pip install -e .[financial]

# Interface graphique moderne
# Borne haute : voir le commentaire dans pyproject.toml (API privee
# remplacee par gui/ctk_patch.py).
customtkinter>=5.2.0,<5.3

# Traitement d'images (pour le logo)
Pillow>=9.0.0

# Manipulation de fichiers Excel
openpyxl>=3.0.0

# Validation de numéros de téléphone
phonenumbers>=8.12.0

# Requêtes HTTP pour les taux de change
requests>=2.25.0

# Génération de PDF professionnels pour les devis
reportlab>=3.6.0

# Note: tkinter est inclus dans Python standard
# Note: Les autres dépendances (os, datetime, re) sont incluses dans Python standard