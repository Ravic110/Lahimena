# Testing dependencies for Lahimena Tours project

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# For test utilities
pytest-timeout>=2.1.0

# For mocking and fixtures
mock>=4.0.3

# Code quality and coverage
# 7.13.4 est cassee : coverage/__init__.py importe control (l.14) avant de lier
# __version__ (l.22), et control -> jsonreport fait `from coverage import __version__`.
# Resultat : ImportError des `import coverage`. Exclue tant qu'elle n'est pas corrigee.
coverage>=6.0,!=7.13.4
